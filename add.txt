Se requiere agregar:

- Validación de los campos de correo
- Validación de que el usuario no se repita
- Validación de formularios


https://www.section.io/engineering-education/how-to-build-authentication-api-with-jwt-token-in-nodejs/

helper.compare(password, user.password).then((val) => {
            if(val==false){
                res.status(404).json({message:"Password incorrecta!!"});
            }else{
                res.status(200).json({message:"password correcta!!"});
            }
            console.log(val)
        });